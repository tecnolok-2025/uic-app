# Render env vars (API)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:info@uic-campana.com.ar
CRON_TOKEN=pon_un_secreto_largo
ALLOWED_ORIGINS=https://TU-PWA.onrender.com

# WordPress REST API base (read-only)
WP_BASE=https://uic-campana.com.ar/wp-json/wp/v2

# If your WP blocks public REST API (e.g. iThemes Security), configure ONE option:
# Option A) Provide an already base64 encoded token of "user:app_password"
# WP_AUTH_B64=
# Option B) Or provide user + app password (we will encode at runtime)
# WP_USER=
# WP_APP_PASSWORD=
