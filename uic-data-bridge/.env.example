# Base Neon actual de UIC (origen / solo lectura del bridge)
PRIMARY_DATABASE_URL=
# Nuevo proyecto Neon Free (destino / respaldo)
SECONDARY_DATABASE_URL=
# Secreto largo para ejecutar /sync manualmente
BRIDGE_TOKEN=
# 15 minutos por defecto; mínimo 5 minutos
SYNC_INTERVAL_MS=900000
PORT=10000
DATABASE_SSL=true
